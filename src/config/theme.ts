import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#04323A",
    },
    secondary: {
      main: "#E2DAD7",
    },
    tertiary: {
      main: "#1C8EA4",
    },
  },
});

export default theme;
